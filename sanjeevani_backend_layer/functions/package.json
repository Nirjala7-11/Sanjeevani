{
  "name": "sanjeevani-functions",
  "version": "1.0.0",
  "description": "Cloud Functions for Sanjeevani backend",
  "main": "src/index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "serve":  "firebase emulators:start --only functions,firestore",
    "test":   "node --test test/"
  },
  "engines": { "node": "20" },
  "dependencies": {
    "firebase-admin":    "^12.0.0",
    "firebase-functions": "^5.0.0"
  },
  "devDependencies": {
    "firebase-functions-test": "^3.1.0"
  }
}
